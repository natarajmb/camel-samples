This is a sample project to demonstrate the usage of Spring Web Service as a camel endpoint.

Steps to run the sample

Step1:
Add following line to MAVEN_OPTS, if its not already there. Below is for debug and it enable all maven applications to be
debugged.

MAVEN_OPTS="-Xmx512m -Xdebug -Xnoagent -Djava.compiler=NONE -Xrunjdwp:transport=dt_socket,address=8000,server=y,suspend=n"

Step2:
mvn clean compile jetty:run

Step3: Load the SoapUI with following URL and make calls
http://localhost:8080/BookService/Books.wsdl

